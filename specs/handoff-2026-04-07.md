# Session Handoff — 2026-04-07

## Context
AIクリエイター（`stages/03_development/manga_creator/`）の詳細設計書を `dev/products/ai-creator/CLAUDE.md` に作成。
Antigravityからの継続開発を可能にするための状態記録。

## 現在の状態

- **稼働中**: port:5173（Vite dev）/ port:5001（Flask）
- **3ツール稼働確認済み**: AI漫画クリエイター / AIスライドクリエイター / AIデザインクリエイター
- **設計書作成済み**: `dev/products/ai-creator/CLAUDE.md`

## すぐに着手できるタスク（Goサイン待ち）

### Task A: ソース移行（30min）
```bash
cp -r ~/Claude/stages/03_development/manga_creator/ ~/Claude/dev/products/ai-creator/src/
# 起動スクリプトのパス更新
# CLAUDE.md更新
```

### Task B: 動画エクスポート実装（2-3h）
- `@remotion/renderer` インストール
- `/api/export-video` エンドポイント追加（`app.py` または新 `routers/video.py`）
- フロントエンドに「MP4ダウンロード」ボタン追加

### Task C: バックエンド Blueprint分割（3-4h）
```
app.py（1928行）→ Flask Blueprint で分割
├── routers/manga.py
├── routers/slide.py
├── routers/design.py
└── routers/templates.py
```

## 技術的な注意点

1. **nlm CLI**: `nlm login` の認証状態が切れると `/api/slide/create` が失敗する
2. **Imagen 4 API**: `imagen-4.0-generate-001` は quota制限あり（大量生成時は注意）
3. **Remotion**: React 19 + Remotion 4 の組み合わせ。`src/RemotionEditor.jsx` のみReact
4. **Supabase**: 現状ローカルの`user_templates/`と併用。環境変数未設定でもローカルのみで動作する

## Key Files

- `stages/03_development/manga_creator/app.py` — バックエンド全API（1928行）
- `stages/03_development/manga_creator/main.js` — フロントエンド全ロジック（3074行）
- `stages/03_development/manga_creator/index.html` — SPA HTML（スクリーン切替）
- `stages/03_development/manga_creator/src/RemotionEditor.jsx` — 動画プレビュー
- `dev/products/ai-creator/CLAUDE.md` — **設計書（このセッションで作成）**
